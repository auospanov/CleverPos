# Deprecated for local Docker Desktop.
# Use deploy.sh / deploy.cmd — build on the server via SSH (password prompt).
#
#   cd CleverPos.License.Api
#   .\deploy.cmd
#   # or: bash deploy.sh

Write-Host "Use deploy.cmd / bash deploy.sh (SSH to server, password will be asked)." -ForegroundColor Yellow
Write-Host "This PowerShell+local Docker path is disabled." -ForegroundColor Yellow
exit 1
