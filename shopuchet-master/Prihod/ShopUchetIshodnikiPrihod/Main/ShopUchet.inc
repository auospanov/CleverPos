{ ShopUchet.inc включается во все модули, так что в нем можно
  указать глобальные директивы компилятору.
  Они могут быть переопределены в конкретных файлах. }

{$DEFINE USE_JCL_DEBUG}


{$DEFINE DEBUG}
{.$DEFINE WRITELOG}

{$DEFINE AUTOLOGON}
